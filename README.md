# Discord Voice Channel Notification Bot

複数のDiscordサーバーでボイスチャンネルの参加・退出・移動を通知するボットです。

## 機能

- 🔊 ボイスチャンネルの参加・退出・移動を自動通知
- 🌐 複数サーバー対応
- ⚙️ サーバー毎の個別設定
- 🕐 タイムゾーン対応
- 🎛️ 通知の有効/無効切り替え
- 💬 わかりやすい日本語メッセージ
- 🔄 **設定の動的再読み込み（再起動不要）**
- 🔒 **スレッドセーフな設定管理**
- 🧩 **Cogベースのモジュラー構成**
- 📊 **豊富な情報コマンド**

## 最新バージョン

- Discord.py: 2.5.2
- 複数サーバー対応
- 改良されたユーザーインターフェース

## セットアップ

### 1. 必要なパッケージのインストール

```bash
pip install -r requirements.txt
```

### 2. Discord Botトークンの設定

環境変数 `BOT_TOKEN` にDiscord Botのトークンを設定してください。

```bash
export BOT_TOKEN=your_bot_token_here
```

### 3. ボットの起動

```bash
python bot.py
```

## 使用方法

### 初期設定

1. ボットをサーバーに招待
2. 通知を受け取りたいチャンネルで `!setup` コマンドを実行
3. 設定完了！

### コマンド一覧

### 設定コマンド

| コマンド | 説明 | 必要権限 |
|---------|------|---------|
| `!setup [#チャンネル]` | 通知チャンネルを設定 | サーバー管理 |
| `!enable` | 通知を有効化 | サーバー管理 |
| `!disable` | 通知を無効化 | サーバー管理 |
| `!status` | 現在の設定を確認 | なし |
| `!timezone <オフセット>` | タイムゾーンを設定 | サーバー管理 |
| `!reload` | 設定を手動再読み込み | サーバー管理 |

### 情報コマンド

| コマンド | 説明 | 必要権限 |
|---------|------|---------|
| `!vcstats` | ボイスチャンネル統計 | なし |
| `!botinfo` | ボット情報 | なし |
| `!ping` | 応答速度確認 | なし |
| `!version` | バージョン情報 | なし |
| `!help` | ヘルプを表示 | なし |

### 設定例

```bash
# 現在のチャンネルを通知先に設定
!setup

# 特定のチャンネルを通知先に設定
!setup #voice-log

# 現在の設定を確認
!status

# 通知を無効化
!disable

# 通知を有効化
!enable
```

## ファイル構成

```
discord-voice-bot/
├── bot.py                    # メインボットファイル
├── cogs/                     # Cogパッケージ
│   ├── __init__.py
│   ├── config_cog.py        # 設定関連コマンド
│   ├── voice_cog.py         # ボイスチャンネル処理
│   └── utility_cog.py       # 情報・ユーティリティコマンド
├── config.json              # 設定ファイル（自動生成）
├── config.json.template     # 設定ファイルのテンプレート
├── requirements.txt         # Python依存関係
├── Dockerfile              # Docker設定
└── README.md               # このファイル
```

## 設定ファイル

`config.json` ファイルでサーバー毎の設定を管理します。

```json
{
    "servers": {
        "サーバーID": {
            "channel_id": "通知チャンネルID",
            "enabled": true,
            "timezone_offset": 9
        }
    },
    "default_settings": {
        "enabled": true,
        "timezone_offset": 9
    }
}
```

## Docker使用

```bash
# Dockerイメージをビルド
docker build -t discord-voice-bot .

# コンテナを起動
docker run -e BOT_TOKEN=your_token_here discord-voice-bot
```

## 必要な権限

ボットに以下の権限を付与してください：

- メッセージを送信
- メッセージ履歴を読む
- 埋め込みリンク
- ボイスチャンネルの状態を表示

## 通知メッセージの例

```
2024/01/01 12:00:00 ユーザー名 が 一般ボイス に参加しました
2024/01/01 12:05:00 ユーザー名 が 一般ボイス から退出しました
2024/01/01 12:10:00 ユーザー名 が 一般ボイス から 会議室 に移動しました
```

## 重要な改善点

### 🔄 動的設定再読み込み
- **再起動不要**：サーバー設定を変更しても、ボットを再起動する必要がありません
- **自動監視**：設定ファイルの変更を30秒ごとに自動チェック
- **手動再読み込み**：`!reload` コマンドで即座に設定を反映可能

### 🔒 スレッドセーフな設定管理
- **競合状態の回避**：複数サーバーからの同時アクセスでも安全
- **データ整合性**：設定の読み書きが確実に同期される
- **エラーハンドリング**：設定ファイルの破損や読み込みエラーに対応

### 💾 設定の自動バックアップ
- **設定の永続化**：すべての設定変更が自動的にファイルに保存
- **エラー耐性**：設定ファイルの読み込みに失敗してもデフォルト設定で継続

### 🧩 Cogベースのモジュラー構成
- **ConfigCog**：設定関連のコマンドを管理
- **VoiceCog**：ボイスチャンネルイベント処理
- **UtilityCog**：情報・ユーティリティコマンド
- **コードの分離**：機能別にコードが整理され、保守性が向上
- **拡張性**：新しい機能を簡単に追加可能

## 変更履歴

### v2.1.0 (最新)
- 🔄 **設定の動的再読み込み機能を追加**
- 🔒 **スレッドセーフな設定管理を実装**
- 🧩 **Cogベースのモジュラー構成に全面改修**
- 📊 **豊富な情報コマンドを追加**
- 📈 **パフォーマンス改善**
- 🛡️ **エラーハンドリングの強化**
- 📋 **新しいコマンドを追加**
  - `!timezone` - タイムゾーン設定
  - `!vcstats` - ボイスチャンネル統計
  - `!botinfo` - ボット情報
  - `!ping` - 応答速度確認
  - `!version` - バージョン情報
  - `!reload` - 設定を手動再読み込み

### v2.0.0
- Discord.py 2.5.2にアップデート
- 複数サーバー対応
- 設定コマンドの追加
- エラーハンドリングの改善
- 日本語メッセージの改善

### v1.0.0
- 初期リリース
- 単一サーバー対応

## よくある質問（FAQ）

### Q: 設定を変更した後、ボットを再起動する必要はありますか？
A: **不要です**。v2.1.0以降では設定が自動的に再読み込みされます。手動で即座に反映したい場合は `!reload` コマンドを使用できます。

### Q: 複数のサーバーで同時に使用できますか？
A: **はい**。各サーバーで個別に設定を行うことで、複数のサーバーで同時に使用できます。

### Q: 設定ファイルを直接編集できますか？
A: **はい**。`config.json` ファイルを直接編集することも可能です。変更は最大30秒以内に自動的に反映されます。

### Q: 設定が正しく反映されない場合は？
A: `!reload` コマンドを試すか、`!status` コマンドで現在の設定を確認してください。

## トラブルシューティング

### 設定ファイルが破損した場合
- ボットは自動的にデフォルト設定で動作を継続します
- `!setup` コマンドで再設定を行ってください

### 権限エラーが発生する場合
- ボットに「メッセージを送信」「メッセージ履歴を読む」「埋め込みリンク」「ボイスチャンネルの状態を表示」権限が付与されているか確認してください

### 通知が届かない場合
- `!status` コマンドで設定を確認してください
- 通知チャンネルが削除されていないか確認してください
- `!enable` コマンドで通知が有効になっているか確認してください

## ライセンス

このプロジェクトはMITライセンスの下で公開されています。
